import React from 'react';
import { Layout } from 'antd';

const Home = (): React.ReactElement => {
  return (
    <Layout className="h-screen p-8">
      <h1>Your lists</h1>
    </Layout>
  );
};

export default Home;
